# sheet: symbaroum
fields:
  ### row 1 --------------
  # row
    # col: col-12 col-md-8
      # card: personal : Symbaroum Character
        # card-body
          # row
            # col: col-12 col-md-8
              # table
  name:
    label: "name"
    input: string
    required: true
  player:
    label: "player"
    input: string
  testbox:
    label: testbox
    input: checkbox
  race:
    label: "race"
    input: string
    required: true
  occupation:
    label: "occupation"
    input: string
    required: true
  shadow:
    label: "shadow"
    input: string
  quote:
    label: "quote"
    input: string
              # /table
            # /col
            # col: col-12 col-md-4
              # table
  age:
    label: "age"
    input: integer
  height:
    label: "height"
    input: string
  weight:
    label: "weight"
    input: string
  xp:
    label: "xp"
    input: integer
  xp unspent:
    label: "xp unspent"
    input: integer
              # /table
            # /col
          # /row
        # /card-body
      # /card
    # /col
    # col: col-12 col-md-4
      # card: portrait
        # card-body
  portrait:
    label: "portrait"
    input: integer
        # /card-body
      # /card
    # /col
  # /row
  # row
    # col: col-12 col-md-8
      # card: attributes: attributes
        # card-body
          # row
            # col: col-6 col-md-3
              # card: accurate
                # card-body
  accurate:
    label: "accurate"
    input: integer
    required: true
                # /card-body
              # /card
            # /col
            # col: col-6 col-md-3
              # card: cunning
                # card-body
  cunning:
    label: "cunning"
    input: integer
    required: true
                # /card-body
              # /card
            # /col
            # col: col-6 col-md-3
              # card: discreet
                # card-body
  discreet:
    label: "discreet"
    input: integer
    required: true
                # /card-body
              # /card
            # /col
            # col: col-6 col-md-3
              # card: persuasive
                # card-body
  persuasive:
    label: "persuasive"
    input: integer
    required: true
                # /card-body
              # /card
            # /col
            # col: col-6 col-md-3
              # card: quick
                # card-body
  quick:
    label: "quick"
    input: integer
    required: true
                # /card-body
              # /card
            # /col
            # col: col-6 col-md-3
              # card: resolute
                # card-body
  resolute:
    label: "resolute"
    input: integer
    required: true
                # /card-body
              # /card
            # /col
            # col: col-6 col-md-3
              # card: strong
                # card-body
  strong:
    label: "strong"
    input: integer
    required: true
                # /card-body
              # /card
            # /col
            # col: col-6 col-md-3
              # card: vigilant
                # card-body
  vigilant:
    label: "vigilant"
    input: integer
    required: true
                # /card-body
              # /card
            # /col
          # /row
        # /card-body
      # /card
    # /col
    # col: col-12 col-md-4
      # row
        # col: col-12
          # card: data: Misc
            # card-body
              # row
                # col: col-6 col-md-6
  pain threshold:
    label: "pain threshold"
    input: integer
                # /col
                # col: col-6 col-md-6
  toughness:
    label: "toughness"
    input: integer
                # /col
                # col: col-6 col-md-6
  toughness max:
    label: "toughness max"
    input: integer
                # /col
                # col: col-6 col-md-6
  corruption:
    label: "corruption"
    input: integer
                # /col
                # col: col-6 col-md-6
  corruption perm:
    label: "corruption perm"
    input: integer
                # /col
                # col: col-6 col-md-6
  corruption threshold:
    label: "corruption threshold"
    input: integer
                # /col
              # /row
            # /card-body
          # /card
        # /col
      # /row
    # /col
  # /row
  ### row 2 --------------
  # row
    # col: col-12
      # card: abilites: Abilities
        # card-body
  ab name:
    label: "ab name"
    input: string
  ab effect:
    label: "ab effect"
    input: text
    rows: 3
  ab type:
    label: "ab type"
    input: string
  ab n:
    label: "ab n"
    input: string
  ab a:
    label: "ab a"
    input: string
  ab m:
    label: "ab m"
    input: string
        # /card-body
      # /card
    # /col
  # /row
  ### row 3 --------------
  # row
    # col: col-12 col-md-6
      # card: weapons: Weapons
        # card-body
          # table
            # iter: 3
  weapon name:
    label: "weapon name"
    input: string
  weapon damage:
    label: "weapon damage"
    input: string
  weapon quality:
    label: "weapon quality"
    input: string
  weapon attribute:
    label: "weapon attribute"
    input: string
            # /iter
          # /table
        # /card-body
      # /card
    # /col
    # col: col-12 col-md-6
      # card: armor: Armor
        # card-body
          # table
            # iter: 2
  armor name:
    label: "armor name"
    input: string
  armor prot:
    label: "armor prot"
    input: string
  armor quality:
    label: "armor quality"
    input: string
  armor defense:
    label: "armor defense"
    input: string
            # /iter
          # /table
        # /card-body
      # /card
    # /col
  # /row
  ### row 4 --------------
  # row
    # col: col-12 col-md-6
      # card: background
        # card-body
  background:
    label: "background"
    input: text
    rows: 3
        # /card-body
      # /card
    # /col
    # col: col-12 col-md-6
      # card: goal
        # card-body
  goal:
    label: "goal"
    input: text
    rows: 3
        # /card-body
      # /card
    # /col
    # col: col-12 col-md-6
      # card: appearance
        # card-body
  appearance:
    label: "appearance"
    input: text
    rows: 3
        # /card-body
      # /card
    # /col
    # col: col-12 col-md-6
      # card: belongings: Belongings
        # card-body
  equipment:
    label: "equipment"
    input: text
    rows: 3
  money:
    label: "money"
    input: string
  assets:
    label: "assets"
    input: text
    rows: 3
        # /card-body
      # /card
    # /col
  # /row
  ### row 5 --------------
  # row
    # col: col-12 col-md-6
      # card: friends: Friends
        # card-body
          # table
            # iter: 5
  friend name:
    label: "friend name"
    input: string
  friend race:
    label: "friend race"
    input: string
  friend occupation:
    label: "friend occupation"
    input: string
  friend player:
    label: "friend player"
    input: string
            # /iter
          # /table
  group name:
    label: "group name"
    input: string
  group goal:
    label: "group goal"
    input: string
        # /card-body
      # /card
    # /col
    # col: col-12 col-md-6
      # card: artifacts: Artifacts
        # card-body
          # table
            # iter: 4
  art name:
    label: "art name"
    input: string
  art power:
    label: "art power"
    input: string
  art corruption:
    label: "art corruption"
    input: string
            # /iter
          # /table
        # /card-body
      # /card
    # /col
  # /row
# /sheet